1. jquery왜 쓰는가? 대표적인 기호는?
  - 웹사이트에 자바스크립트를 쉽게 활용할 수 있도록 도와주는 오픈소스 기반의 자바스크립트 라이브러리
  - $
  
2. ajax는 무엇인가? 왜쓰는가?
  - Ajax는 빠르게 동작하는 동적인 웹 페이지를 만들기 위한 개발 기법의 하나
  - Ajax는 웹 페이지 전체를 다시 로딩하지 않고도, 웹 페이지의 일부분만을 갱신할 수 있음
  - URL로 외부 페이지와 통신할 수 있음
  
3. ajax는 무엇의 약자?
  - Asynchronous JavaScript and XML
  
4. 동기와 비동기란 무엇인가?
  - 데이터를 받는 방식 중 요청과 그 결과가 동시에 일어나는 방식이 동기(Synchronous),
  - 요청한 그 자리에서 결과가 주어지지 않는 방식이 비동기(Asynchronous)

5. ajax는 비동기통신인가? 동기통신인가?
  - 비동기통신
  
6. ajax함수의 입력값의 형태는? ($.ajax(????))
   - JSON 타입의 입력값을 받는다
   
7. JSON은 무엇의 약자? 자바의 어떤 형태와 동일한 자료의 형태(자료구조)인가?
  - JavaScript Object Notation
  - 자바의 해시맵, 파이썬의 딕셔너리와 동일한 자료형태

8. ajax함수의 JSON형태에서 주로 사용되는 필수 키 3가지는?
  - url, data, success, (fail)

9. ajax함수의 JSON형태에서 주로 사용되는 필수 키 중 data키는 어떤 역할인가?
  - url로 data를 전달하는 역할

10. ajax함수의 JSON형태에서 주로 사용되는 필수 키 중 success키의 값은?
  - function (함수)
  
11. 간단히 말해서 JSON형태라는 것은 어떤 자료구조를 말하는가?
  - "{속성:값}" 또는 "{키-값}"의 쌍으로 이루어진 데이터 오브젝트를 전달하기 위한 자료형

12. jquery를 사용하고 싶다. 나는 어떤 설정을 해야하는가?
  - jQuery를 script 태그로 추가

13. $(????): ????에 올 수 있는 기본 선택자3개는?
  - id, tag, class

14. $(????): ????에 올 수 있는 기본 선택자 3개의 사용법은?
  - id      $('#id')
  - tag     $('tag')
  - class   $('.class')

15. 화면을 넘기지 않고 서버에 요청을 원할 때 사용하는 방법으로 어떤 것을 사용하겠는가?
  - Ajax
